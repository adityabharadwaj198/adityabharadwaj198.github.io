<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="4.3.4">Jekyll</generator><link href="http://localhost:4000/feed.xml" rel="self" type="application/atom+xml" /><link href="http://localhost:4000/" rel="alternate" type="text/html" /><updated>2026-07-20T21:01:02+05:30</updated><id>http://localhost:4000/feed.xml</id><title type="html">Aditya Bharadwaj</title><subtitle>Technology Brother</subtitle><entry><title type="html">Recent updates: search, retrieval, and learning ML properly</title><link href="http://localhost:4000/blog/2026/07/20/recent-updates.html" rel="alternate" type="text/html" title="Recent updates: search, retrieval, and learning ML properly" /><published>2026-07-20T00:00:00+05:30</published><updated>2026-07-20T00:00:00+05:30</updated><id>http://localhost:4000/blog/2026/07/20/recent-updates</id><content type="html" xml:base="http://localhost:4000/blog/2026/07/20/recent-updates.html"><![CDATA[<p>It’s been a minute.</p>

<p>I joined Uber Eats in September, on the Search team. We focus on building the data pipelines that serve Uber’s ranking and retrieval use cases—both offline training and online inferencing.</p>

<p>From December through April, I worked on pipelines that generate embeddings for store and item entities, then index them in a Lucene-based vector index for approximate nearest-neighbour search, specifically HNSW.</p>

<p>We also built more frequent delta pipelines for updates, additions, and deletes. These pipelines run inference to generate embeddings, publish them to a Kafka topic, and have a consumer ingest them into the vector index.</p>

<p>I enjoyed this work a lot. It gave me a much clearer view of what it takes to move from an embedding model to a production retrieval system.</p>

<h2 id="things-i-learned">Things I learned</h2>

<p>Some of the topics I encountered—and want to understand more deeply—were:</p>

<ol>
  <li><strong>Two-tower embedding models.</strong> I understand the broad architecture, but I want to study them more deeply.</li>
  <li><strong>Quantisation in vector indexes.</strong></li>
  <li><strong>The search funnel:</strong> retrieval-based candidate generation, first-pass ranking, second-pass ranking, hydration, and filtering.</li>
  <li><strong>Data leakage in ranking models.</strong></li>
  <li><strong>Online versus offline features.</strong> Online inference can use real-time databases such as Cassandra or Redis, while offline feature generation and aggregation can use systems such as Hive.</li>
  <li><strong>The feature lifecycle:</strong> feature generation, aggregation, ingestion into an offline database, ingestion into an online database, and finally feature retrieval at prediction time.</li>
  <li><strong>Semantic IDs and the cold-start problem.</strong></li>
  <li><strong>Ways to generate Semantic IDs,</strong> including residual quantisation, RQ-optimised product quantisation, and RQ-VAE.</li>
  <li><strong>Sequence modelling:</strong> representing a customer’s sequence of actions to predict what they might do next.</li>
  <li><strong>Generative retrieval and ranking using Semantic IDs.</strong></li>
  <li><strong>Feature unification.</strong> Putting the features needed for model inference into a single Cassandra or Redis table can reduce latency by bringing related data into the cache together and serving subsequent requests from it.</li>
</ol>

<h2 id="semantic-ids-and-generative-retrieval">Semantic IDs and generative retrieval</h2>

<p>Semantic IDs are one of the areas I want to explore next. I plan to read <a href="https://papers.neurips.cc/paper_files/paper/2023/file/20dcab0f14046a5c6b02b61da9f13229-Paper-Conference.pdf"><em>Recommender Systems with Generative Retrieval</em></a> and experiment with Semantic IDs on my own data.</p>

<p>The idea of replacing arbitrary item identifiers with IDs that encode semantic information is fascinating. It connects representation learning, quantisation, retrieval, and generation in a way that feels both theoretically interesting and practically useful.</p>

<h2 id="what-im-reading">What I’m reading</h2>

<p>I’m currently reading:</p>

<ul>
  <li><em>AI Engineering</em> by Chip Huyen</li>
  <li><em>Hands-On Large Language Models</em> by Jay Alammar and Maarten Grootendorst</li>
</ul>

<p>I also have Chip Huyen’s <a href="https://huyenchip.com/machine-learning-systems-design/design-a-machine-learning-system.html#project-setup-zlkQIG9">guide to designing a machine learning system</a> and <a href="https://huyenchip.com/ml-interviews-book/contents/0-about-the-questions.html">Machine Learning Interviews Book</a> on my radar.</p>

<h2 id="learning-ml-from-the-foundations">Learning ML from the foundations</h2>

<p>I’ve also been working through Andrej Karpathy’s <em>Neural Networks: Zero to Hero</em> course. So far, I have:</p>

<ul>
  <li>Built a bigram character-level language model from scratch</li>
  <li>Implemented a trigram character-level language model using sampling</li>
  <li>Implemented some neural networks from scratch</li>
</ul>

<p>I intend to return to the course and eventually be able to build GPT from scratch. I’ll probably share my solutions to the exercises as I go.</p>

<p>Going through the course made me realise that I never studied ML or AI properly at university. I want to fix that, so today I revisited some material from Andrew Ng’s Deep Learning Specialization. I intend to implement the exercises from that course, advice that I got ~8 years back in college from my friends.</p>

<p>I also want to learn more about inference engineering. I can already think of a few interesting problems at Uber where that knowledge could be useful.</p>

<h2 id="system-design-implementations">System design implementations</h2>

<p>Last year when I was interviewing I implemented a few toy systems and they helped me remember things really well. Like consistent hashing - so this year I’m planning to take it forward by implementing LSM tree on my own. I’m procrastinating a lot on that because I can’t figure out how skip lists work without having to look at someone’s blog.</p>

<h2 id="elsewhere">Elsewhere</h2>

<p>I’m LeetCoding on the side because my stupid ass applied to a really nice company, got a call back, and would hate to interview without being prepared.</p>

<p>Outside work and study, I hit two reps at 95 kg (209 lb) on the bench. I’ve been enjoying training in the gym again after a long time.</p>

<p>I’m also guiding my little sister on ML / AI and having fun discussing topics with her that she’s gotten more familiar with than me lol. I’ve also been funding her robotics pursuits. This makes me want to buy my own drone and program it.</p>

<p>I’m also trying to practice delusional optimism a whole lot more these days.</p>]]></content><author><name></name></author><category term="blog" /><category term="machine-learning" /><category term="search" /><category term="retrieval" /><category term="embeddings" /><category term="learning" /><summary type="html"><![CDATA[What I've been working on at Uber Eats, the ML and retrieval ideas I've been learning, and what I want to explore next.]]></summary></entry><entry><title type="html">What the heck is the METEOR Score (and why I added it to Promptfoo)</title><link href="http://localhost:4000/blog/2025/07/25/meteor-score.html" rel="alternate" type="text/html" title="What the heck is the METEOR Score (and why I added it to Promptfoo)" /><published>2025-07-25T00:00:00+05:30</published><updated>2025-07-25T00:00:00+05:30</updated><id>http://localhost:4000/blog/2025/07/25/meteor-score</id><content type="html" xml:base="http://localhost:4000/blog/2025/07/25/meteor-score.html"><![CDATA[<p>I was recently going over Chip Huyen’s AI engineering book (if you want a quick beginner friendly compilation you should check out the book). In Chapter 3 of the book titled Evaluation Methodology, she mentions different kinds of Lexical similarity metrics - which include BLEU, METEOR &amp; ROUGE. It reminded me of the time when I implemented the METEOR metric for a eval + ai red teaming tool called Promptfoo. Let’s dive into it.</p>

<h2 id="first-off--what-is-meteor">First off — what is METEOR?</h2>

<p>METEOR stands for Metric for Evaluation of Translation with Explicit ORdering. Cool acronym, what does it do? idea behind it is:</p>

<p>“How similar is the model’s output to what I wanted it to say?”</p>

<p>It’s not just a simple string match. METEOR goes beyond that, it checks if the words match, whether they have the same root (stems), and even whether they’re synonyms. Oh, and it also factors in the word order.</p>

<h2 id="so-why-did-i-build-this-for-promptfoo">So why did I build this for Promptfoo?</h2>
<p>Promptfoo is a powerful open-source framework to test and benchmark LLM outputs. It supports all kinds of evaluation methods — from latency to cost to BLEU (I implemented this too!), GLEU, ROUGE and even model-graded evaluations.</p>

<p>But there was a missing piece in the lexical similarity toolkit. What we needed was a metric that:</p>

<p>Works well for short, structured outputs (like translations or summaries)
Can account for minor word reordering
Supports multiple valid reference outputs
Understands synonyms and stems</p>

<p><em>Oh, and they had an open issue where a Promptfoo user asked for being able to use the METEOR metric lol.</em></p>

<p>So I dived right into it and added METEOR support to Promptfoo.</p>

<h2 id="how-meteor-works-with-examples">How METEOR works (with examples)</h2>

<p>At a high level, METEOR compares a model-generated sentence with a reference by matching words in three stages:</p>

<ol>
  <li>
    <p>Exact word match</p>
  </li>
  <li>
    <p>Stemming match (e.g., “running” vs. “run”)</p>
  </li>
  <li>
    <p>Synonym match (e.g., “nice” vs. “beautiful”, using WordNet)</p>
  </li>
</ol>

<p>Once matches are found, it calculates precision and recall, applies a penalty for how fragmented the matches are (i.e. word order differences), and outputs a final score between 0 and 1.</p>

<p>Here’s a quick example:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Reference:
The weather is beautiful today

Candidate outputs and expected scores (approximate):

The weather is beautiful today → 1.0 (exact match)

Today's weather is beautiful → ~0.85 (same words, reordered)

The weather is nice today → ~0.7 (synonym match on "nice")

It is sunny outside → ~0.3 (semantic drift, different words)
</code></pre></div></div>
<p>This kind of scoring is especially useful when there’s more than one “acceptable” phrasing — something that comes up constantly in LLM outputs.</p>

<p>Unlike BLEU or Levenshtein, METEOR gives partial credit for “close enough” matches while still penalizing structural changes when they matter.</p>

<h2 id="how-to-use-meteor-in-promptfoo">How to use METEOR in Promptfoo</h2>

<p>I wrote some documentation as part of my PR as well so I may as well point you directly to the documentation: https://www.promptfoo.dev/docs/configuration/expected-outputs/deterministic/#meteor</p>

<h2 id="when-should-you-use-meteor">When should you use METEOR?</h2>
<p>If your tests are judging:</p>

<ul>
  <li>Summaries</li>
  <li>Translations</li>
  <li>Short natural language answers</li>
</ul>

<p>Anywhere there’s more than one “valid” way to say something
…it’s a great tool.</p>

<h2 id="final-thoughts">Final Thoughts</h2>
<p>Adding METEOR to Promptfoo was fun, both as a coding problem (chunking, synonyms, scoring!) and as a UX enhancement for LLM evaluators. I usually don’t code in Typescript so it was a great exercise in that sense! Here’s my PR if interested to check it out: https://github.com/promptfoo/promptfoo/pull/3776/files</p>

<p>If you want to go beyond BLEU and get a more nuanced picture of output quality, give it a spin. Your tests will thank you. And your models might pass more often.</p>]]></content><author><name></name></author><category term="blog" /><category term="ai" /><category term="evals" /><category term="promptfoo" /><category term="lexical-similarity" /><summary type="html"><![CDATA[A quick dive into METEOR, a smarter evaluation metric for LLM outputs, and a behind-the-scenes look at how I implemented it for Promptfoo.]]></summary></entry><entry><title type="html">QuickSelect</title><link href="http://localhost:4000/blog/2025/07/12/quickselect.html" rel="alternate" type="text/html" title="QuickSelect" /><published>2025-07-12T00:00:00+05:30</published><updated>2025-07-12T00:00:00+05:30</updated><id>http://localhost:4000/blog/2025/07/12/quickselect</id><content type="html" xml:base="http://localhost:4000/blog/2025/07/12/quickselect.html"><![CDATA[<p>How to write QuickSelect &amp; how to avoid worst case time complexity of O(N²)</p>

<h2 id="quickselect">QuickSelect</h2>

<p>Quickselect is a useful algorithm usually used for problems like <a href="https://leetcode.com/problems/kth-largest-element-in-an-array">“Kth largest element”</a> or “Kth smallest element” (Unfortunately there’s no question like this on leetcode but you get the gist). It would be helpful to checkout this <a href="https://leetcode.com/problem-list/quickselect/">QuickSelect</a> tag on LC to see the type of problems QuickSelect can help with.</p>

<p>You might be thinking but these problems can be solved with a max heap or a min heap right? Right. And a heap makes it fairly easy too, just identify the type of problem (Kth largest will use a min heap &amp; Kth smallest will use a max heap) and solve it in O(NlogK) time, and be done with it.</p>

<p>However, life isn’t this easy always, and some of the interviewers (Meta <em>cough cough</em>) expect a QuickSelect solution to these problems. I can’t even blame them, considering that the average case complexity of such a algorithm is O(N). Then again with a small enough K, the heap solution could be O(N) as well but you get what I mean.</p>

<p>So let’s go over how a QuickSelect algorithm works and how to derive it on the spot.</p>

<p>QuickSelect is from a family of algorithms (or rather a nuclear family considering the size) that are based on QuickSort. 
Now, how does QuickSelect work?</p>

<p>Imagine we’re solving this problem: 
<a href="https://leetcode.com/problems/kth-largest-element-in-an-array">“Kth largest element”</a></p>

<p>Here, we want to find the Kth largest element. In a monotonically increasing array, the Kth largest element will always lie at the N - kth index (N being the length). 
So we do exactly that, we look at the N - kth index from the start, while we try to place numbers in their correct position using the Quickselect algorithm. When we find an element who was supposed to be at index N - k, we return that number - since we are sure that Quickselect would’ve positioned the element correctly in the array.
More specifically, we look at using QuickSelect to correctly place an element at the N - kth position such that all the elements before the N - kth position are smaller than the element at arr[n - k], and all the elements after index n - k, are greater than arr[n - k].</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class Solution {
    public int findKthLargest(int[] nums, int k) {
        
        // if you want to search for kth largest element in a sorted (ascendingly sorted) array 
        // you gotta look for n-kth element from the start. 
        // Let's write a method getPartitionIndex(int[] nums, int l, int r) which whose responsibility is to always give return you the position at which "a pivot that the recurse method chooses". This position is called a partitionIndex, because you are effectively partitioning the array into 
            1. elements lower than or equal to pivot 
            2. elements higher than pivot. 

        How do you select a pivot you ask? 
        // There are multiple pivot selection strategies. Some algorithms suggest selecting the first element, some select the last element. 
        // However, these strategies could result in a worst case time complexity of O(N), where you are consistently selecting the largest number (this is specifically - in case your strategy is selecting the last element).

        To avoid this, we apply randomised pivot selecting strategy. Sounds esoteric, but it's just about generating a number between l &amp; r, and setting your pivotValue as the element you just generated randomly. More on this inside the getPartitionIndex(int[] nums, int l, int r) method [notice then name change?].


        int l = 0, r = nums.length - 1; // initialize the high level variables 
        
        while (l &lt;= r) { 
            int partitionIndex = getPartitionIndex(nums, l, r); // getPartitionIndex (the index where all elements left of the selected pivot are smaller than equal to pivot, and all elements right of the pivot are greater than equal to pivot)
            if (partitionIndex == nums.length - k) {
                return nums[partitionIndex];
            } else if (partitionIndex &lt; nums.length - k) {
                l = partitionIndex + 1;
            } else {
                r = partitionIndex - 1;
            }
        }
        return nums[nums.length - k];
    }



    public int getPartitionIndex(int[] nums, int l, int r) {
        /*
        An example: 
        [3,2,1,5,6,4]

        paritionIndex = 0, pivotIndex = 5
        i = 0, nums[i] &lt;= nums[5] (i.e 4) =&gt; swap paritionIndex with i (i.e swap (0, 0)). Increment partitionIndex
        
        paritionIndex becomes 1, i = 1. 
        nums[1] &lt;= nums[5] (which is 4) =&gt; swap partionIndex with i (i.e swap (1, 1)). Increment partitionIndex
        
        paritionIndex becomes 2, i becomes 2
        nums[2] &lt;= nums[5] (nums[5] is 4) =&gt; swap partionIndex with i (i.e swap (2, 2)). Increment partitionIndex

        paritionIndex becomes 3, i becomes 3 
        nums[3] &gt; nums[5] (nums[5] was 4) =&gt; can't swap. partitionIndex should not be incremented. i gets incremented and becomes 4 

        paritionIndex stays 3, i becomes 4 
        nums[4] &gt; nums[5] (nums[5] was 4) =&gt; can't swap. partitionIndex should not be incremented. i gets incremented and becomes 5. 
        
        i runs only till &lt;r which was 4, so exit the loop. 

        At the end, swap partitionIndex &amp; r (r was 5). So we are effectively swapping 5 and 4.
        Finally the array becomes [3, 2, 1, 4, 6, 5]

        Another example: 
        
        -1, -2, 4, 3, 1, 2 
        This is a case where max values are in the middle of the array
        pivotIndex = 5, paritionIndex = 0, i = 0

        paritionIndex = 0, pivotIndex = 5
        i = 0, nums[i] &lt;= nums[5] (i.e 2) =&gt; swap partionIndex with i (i.e swap (0, 0)). Increment partitionIndex

        paritionIndex becomes 1, i becomes 1. 
        i = 1, nums[i] &lt;= nums[5] (i.e 2) =&gt; swap partionIndex with i (i.e swap (1, 1)). Increment partitionIndex

        paritionIndex becomes 2, i becomes 2. 
        i = 2, nums[i] &gt; nums[5] (i.e 2) =&gt; can't swap.  partitionIndex should not be incremented. i gets incremented and becomes 3. 

        paritionIndex stays 2, i becomes 3
        i = 3, nums[i] &gt; nums[5] (i.e 2) =&gt; can't swap. partitionIndex should not be incremented. i gets incremented and becomes 4. 

        paritionIndex stays 2, i becomes 4
        i = 3, nums[i] &lt;= nums[5] (i.e 2) =&gt; swap partionIndex with i (i.e swap (2, 4)). Increment partitionIndex. i gets incremented and becomes 5. 
        
        Array becomes [-1, -2, 1, 3, 4, 2]

        Finally after the for loop. swap partitionIndex &amp; r (r was 5). So we are effectively swapping partitionIndex = 3 and r = 5. 

        Array becomes [-1, -2, 1, 2, 4, 3]. Finally, our chosen pivot (value 2 at the chosen pivotIndex 5), has landed in it's correct sorted place. 
        We return partitionIndex (partitionIndex was 3) back to the function above. 



        */
        /*
            strategy here is to select the pivot as the last element of the array. In that case, pivotIndex = r
            partitionIndex will initialized as l. You will iterate the array, i = starting l to r and if an element at index i  
            is lesser than nums[pivotIndex], then you will swap it with the element at partitionIndex. ParitionIndex thus, is always the index 
            where any element less than equal to nums[pivotIndex] (can be called pivotValue) needs to go. Once we emplace a 
            element that should be at a certain partitionIndex by swapping, we need to increment partitionIndex.
            We do the incrementing because all elelements at / before that partitionIndex are guaranteed to have a value less than 
            paritionIndex, any newer elements need to be emplaced at the next partitionIndex which will be the incremented value. 

            At the end we swap the partitionIndex value with pivotIndex value. Remember that nums[pivotIndex] contained pivot, and now since it lies 
            on the partitionIndex value, the pivot lies in it's correct ordered position inside the array. 
        */
        int paritionIndex = l;
        int pivotIndex = r;
        for (int i=l; i&lt;r; i++) { //
            if (nums[i] &lt;= nums[pivotIndex]) {
                swap(nums, i, storeIndex);
                paritionIndex++;
            } 
        }
        swap(nums, paritionIndex, pivotIndex); 
        return paritionIndex;
    }

    public void swap(int[] nums, int i, int j) {
        if (i == j) {
            return;
        }
        int temp = nums[i];
        nums[i] = nums[j];
        nums[j] = temp;
    }
}
</code></pre></div></div>

<p>Ignore the comments if you can. This algorithm’s time complexity is O(N) on an average but quickly shoots up to O(N²) in worst case. Can you think of what that case is?</p>

<p>Similar to QuickSort it’s when the array is already sorted and we keep picking the last element as the pivot. Note that QuickSort’s average case TC is O(NlogN) &amp; not O(N).</p>

<p>Anyway, I digress. Getting back to how to make QuickSelect work in such a manner that it’s TC is always O(N)? In comes something called Randomised QuickSelect (Okay I totally made that up - but Randomised QuickSort exists and there’s no reason why Randomised QuickSelect shouldn’t). Even with a Randomised QuickSelect you can’t always make sure that it’s TC will always be O(N), but it’s highly unlikely.</p>

<p>Here, instead of choosing the same last element again and again as the pivot, we start choosing a random index everytime. This random index should obviously be inside the array.</p>

<p>2 extra steps from the above getPartitionIndex() method is to 
    1. select a random index
    2. send that value all the way back at the end of the array (by swapping the selected random index with r).</p>

<p>The above getPartitionIndex() method would become getPartitionIndexRandomised()</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>
    public int getPartitionIndexRandomised(int[] nums, int l, int r) {
        int paritionIndex = l;
        Random rand = new Random();
        int pivotIndex = l + rand.nextInt(r - l + 1);
        int pivotValue = nums[pivotIndex];
        swap(nums, pivotIndex, r);
        for (int i=l; i&lt;r; i++) { //
            if (nums[i] &lt;= pivotValue) {
                swap(nums, i, paritionIndex);
                paritionIndex++;
            }
        }
        swap(nums, paritionIndex, r); // Swap paritionIndex with r because r is where the pivotValue is (it was swapped earlier)
        return paritionIndex;
    }

</code></pre></div></div>

<p>What are we doing differently here? We’re first of all using a random index as the pivot index every time (and we’re also moving it to the end of the array, this helps keep most of the algorithm same as the non-randomised one)</p>

<p>Now let’s do the time complexity calculation:</p>

<p>On average, the random pivot splits the array reasonably well, say around 25–75%, 50–50%, etc.</p>

<p>So the recurrence is:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>T(n) = T(n/2) + O(n)     // average partitioning + one subarray recurse
Using the Master Theorem or recursive tree analysis, this gives:

T(n/2) = T(n/4) + O(n/2)  
T(n/4) = T(n/8) + O(n/4)  
T(n/8) = T(n/16) + O(n/8)
T(n/2^n) = T(n/2^(n+1)) + O(n/2^n)  
</code></pre></div></div>
<p>Adding all of these will give you</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>T(n) = T(n/2^(n+1)) + O(n) +  O(n/2)  + O(n/4)  + O(n/8) + .... O(n/2^n) 

O(n) +  O(n/2)  + O(n/4)  + O(n/8) + .... O(n/2^n)  = O(2n) (because n/2 + n/4 + n/8 + ... n / 2^n = n)

T(n) = O(2n) ~ O(n)
</code></pre></div></div>

<p>Worst case it could be: O(n²)</p>

<p>Pivot is always the maximum or minimum element</p>

<p>Partitioning gives you subarrays of size n-1 and 0</p>

<p>This gives recurrence:</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>T(n) = T(n - 1) + O(n)
     = O(n²)
</code></pre></div></div>
<p><strong>This is extremely rare if the pivot is randomized, it requires consistent bad luck in pivot selection.</strong></p>

<p>So this tells you that average case time complexity of a quick select algorithm is O(N). Worst case is still O(N²) but it’s likelyhood can be reduced a lot if you do randomised pivot selection.</p>

<p>Neetcode has a really nice explanation for this question: https://youtu.be/XEmy13g1Qxc?si=vXaavNToztXoGrOh</p>]]></content><author><name></name></author><category term="blog" /><category term="algorithms" /><category term="quicksort" /><category term="quickselect" /><category term="leetcode" /><summary type="html"><![CDATA[How to write QuickSelect &amp; how to avoid worst case time complexity of O(N²)]]></summary></entry><entry><title type="html">What the heck is a vector embedding?</title><link href="http://localhost:4000/blog/2025/06/16/vectors.html" rel="alternate" type="text/html" title="What the heck is a vector embedding?" /><published>2025-06-16T00:00:00+05:30</published><updated>2025-06-16T00:00:00+05:30</updated><id>http://localhost:4000/blog/2025/06/16/vectors</id><content type="html" xml:base="http://localhost:4000/blog/2025/06/16/vectors.html"><![CDATA[<p>Vectors, Vector Embeddings, Vector Databases, and HNSW</p>

<h2 id="vectors">Vectors</h2>

<p>Vectors are interesting, they can pretty mean a lot of things in different contexts. In the context of the famed first programming language of every computer science student (C++), they may mean an array of N numbers (where N = 1 to X). In the context of physics they may mean something that has a certain direction. î (magnitude along x axis), ĵ (magnitude along y axis), k̂ (magnitude along z axis). Any point in 3D space can be represented by [x, y, z] coordinates which can be called a vector. We represent a location on the map with a vector of 2 dimensions (latitude and longitude).</p>

<p>To put it in context of the topic that we’re discussing, we can call vectors as arrays of length N, where N might be 3 (for a point in 3D space), 2 (for a point on a map), or even 768, 1024, 1536 etc. We just say N dimensions.</p>

<h2 id="vector-embeddings">Vector embeddings</h2>

<p>Models today are very powerful at one thing, they’re powerful at taking in a arbitrary piece of data (a word, a text, an image, an audio or even a video), and converting them to vector embeddings. These embeddings are just numerical representations of the input (in our case a word, a text, an image, an audio or even a video) that aim to capture meaning of the input, in N dimensions. The value of N depends on the Embedding Model that we’re using, but to generalize we could say that an Embedding Model is good at taking in an input, and spitting out a vector embedding.</p>

<p>Let’s say for an example, that a piece of text “A red dress”, has a vector embedding as: [1, 2, 3], let’s say (again for example) that this was generated by an Embedding  that spits out 3 dimensional embeddings. Models today are so good, that if you actually input an image of a red dress &amp; the Embedding  had to spit out a 3 dimensional embedding, it would give out something like a [1.1, 2.1, 2.9] as an ouput. Do you see how close it is to the original vector of the query? Our model inherently knows that the query “A red dress” and an actual image of the red dress are similar things, and so it outputs similar embeddings for both.</p>

<p>On a side note, a helpful tool to visualise vector embeddings for words in a 3D space is this embedding projector: https://projector.tensorflow.org/</p>

<h3 id="whats-a-embedding-model">What’s a Embedding model?</h3>

<ul>
  <li>I mentioned an Embedding model in a couple of places above, so I thought I should clarify: Embedding Models are Models trained especially to produce embeddings. They include open source models like BERT, CLIP (Constrastive Language-Image Pre-training) and Sentence Transformers.</li>
</ul>

<h2 id="vector-databases">Vector databases</h2>

<p>The bread and butter of vector databases is that a user should be able to put in a query (“A red dress”), and the Vector DB should be able to pull back the proper set of result (The image of the red dress, along with any other things that are similar to “A red dress”) as a response. BTW, a vector database doesn’t need you to tag that image of the red dress with something “red dress” or even “red” or even “dress”.
It has to do this at scale, out of billions of vectors (or how many ever vectors we’re storing).</p>

<p>One of the most popular vector search data structure / algorithm that Vector DBs use is HNSW (Heirarchical Navigable Small Worlds). HNSW sits at the core of systems like Weaviate, Elasticsearch, Vespa, OpenSearch, PGVector, and many others.</p>

<h2 id="hnsw">HNSW</h2>

<p>HNSW is a graph based algorithm used for approximate nearest neighbor (ANN) search in high-dimensional spaces, particularly in vector databases.</p>

<p>It works by starting at an arbitrary node in the graph, and then walking the graph to get closer to the vector embeddings of your input query.</p>

<p>Stay tuned for another article where I go over HNSW in depth!</p>]]></content><author><name></name></author><category term="blog" /><category term="vectors" /><category term="vector-search" /><category term="vector-db" /><category term="hnsw" /><summary type="html"><![CDATA[An introduction to vectors, vector embeddings, and vector databases.]]></summary></entry><entry><title type="html">Welcome to My Blog</title><link href="http://localhost:4000/blog/2024/03/20/welcome-to-my-blog.html" rel="alternate" type="text/html" title="Welcome to My Blog" /><published>2024-03-20T00:00:00+05:30</published><updated>2024-03-20T00:00:00+05:30</updated><id>http://localhost:4000/blog/2024/03/20/welcome-to-my-blog</id><content type="html" xml:base="http://localhost:4000/blog/2024/03/20/welcome-to-my-blog.html"><![CDATA[<p>Get a peek into my mind.</p>

<h2 id="what-to-expect">What to Expect</h2>

<p>In this blog, I’ll be writing about:</p>

<ul>
  <li>Personal projects and experiences</li>
  <li>My experiences with building my own X</li>
  <li>My experiences navigating the software engineering world</li>
  <li>Book / Album reviews and Songs / Artist recommendations</li>
  <li>History? Prehistoric India, IVC, Proto-Indo-Europeans [controversial stuff lol], Vedic age etc.</li>
</ul>

<h2 id="why-i-started-this-blog">Why I Started This Blog</h2>

<p>I am lazy and I never thought of writing anything more than what the educational curriculum 
demanded from me, but I have recently started to journal sometimes just to feel saner. 
I can’t pin-point what it does for me, other than make me feel good in some way.
For all I know it could be placebo. Anywhoo, I keep reading &amp; learning a 
lot of interesting things, that might be of interest in general to the
rest of the world (trust me), and what better way to explain something 
than to write it down in your own words. So this is my attempt at writing.</p>

<p>Through the blog I want to:</p>

<ol>
  <li>Document my learning journey</li>
  <li>Share insights that might help others</li>
  <li>Connect with like-minded individuals</li>
  <li>Build a portfolio of my thoughts and expertise</li>
  <li>Build my own corner of the internet :)</li>
</ol>

<h2 id="stay-connected">Stay Connected</h2>

<p>Feel free to reach out to me through any of the social links in the footer. I’m always happy to connect with fellow developers and technology enthusiasts!</p>

<p><em>Stay tuned for more posts coming soon!</em></p>]]></content><author><name></name></author><category term="blog" /><category term="welcome" /><category term="introduction" /><summary type="html"><![CDATA[Get a peek into my mind. I'll be writing about personal projects, software engineering experiences, book reviews, and even some controversial history topics like prehistoric India and the Vedic age.]]></summary></entry></feed>